apiVersion: v1
kind: Service
metadata:
  name: servicebus-api
spec:
  type: LoadBalancer  # Or use Ingress for more control
  selector:
    app: servicebus-api
  ports:
  - port: 80
    targetPort: 8000
